# Runtime dependencies.
# Floor pins (>=) mark packages whose minimum versions are set by the
# security audit (spec: Athena FabVision v1.0, dependency refresh); pip
# resolves the newest compatible release. Clean pins (==) stay exact.

# --- Security-audited floors -------------------------------------------------
Django>=5.2.17
pillow>=12.3.0
djangorestframework>=3.17.2
aiohttp>=3.14.1
sqlparse>=0.6.0
requests>=2.32.4
protobuf>=5.29.6
h11>=0.16.0

# --- Security refresh (current secure versions) -------------------------------
# httpcore >=1.0.9 is required for the h11 floor to resolve (older httpcore
# caps h11<0.15, which carries the CVE the audit flags).
urllib3>=2.8.0
PyJWT>=2.14.0
idna>=3.20
python-dotenv>=1.2.3
httpcore>=1.0.9

# --- Clean pins (unchanged) ---------------------------------------------------
openai==1.53.0
numpy==2.1.2
gunicorn==23.0.0

# --- LiveKit family (minimal within-0.x bumps forced by the audit) ------------
# livekit-agents <=0.11.1 caps pillow~=10.3.0 via its [images] extra (pulled by
# livekit-plugins-openai), which cannot coexist with the audited pillow>=12.3.0
# floor. 0.12.20 is the newest 0.x release that keeps the current API surface
# while accepting pillow>=12.3.0; the core family moves with it (agents 0.12.x
# requires livekit>=0.18.1, livekit-api~=0.8, livekit-protocol~=0.7).
livekit==0.22.0
livekit-agents==0.12.20
livekit-api==0.8.2
livekit-plugins-openai==0.10.5
livekit-plugins-silero==0.7.3
livekit-protocol==0.9.2

# --- Other dependencies -------------------------------------------------------
aiodns==4.0.4
aiofiles==24.1.0
aiohappyeyeballs>=2.5.0
aiosignal>=1.4.0
annotated-types==0.7.0
anyio==4.6.2.post1
asgiref==3.8.1
async-timeout==4.0.3
attrs==24.2.0
av==13.1.0
certifi==2024.8.30
cffi==1.17.1
charset-normalizer==3.4.0
click==8.5.0
coloredlogs==15.0.1
distro==1.9.0
exceptiongroup==1.2.2
flatbuffers==24.3.25
frozenlist==1.5.0
httpx==0.27.2
humanfriendly==10.0
jiter==0.7.0
mpmath==1.3.0
multidict==6.1.0
onnxruntime==1.20.0
packaging==24.1
propcache==0.2.0
psutil==5.9.8
pycares==5.0.1
pycparser==2.22
pydantic==2.9.2
pydantic_core==2.23.4
python-decouple==3.8
sniffio==1.3.1
sympy==1.13.3
tqdm==4.66.6
types-protobuf==4.25.0.20240417
typing_extensions==4.12.2
watchfiles==0.24.0
yarl==1.17.1
