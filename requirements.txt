# Runtime dependencies.
# Floor pins (>=) mark packages whose minimum versions are set by the
# security audit (spec: Athena FabVision v1.0, dependency refresh); pip
# resolves the newest compatible release. Clean pins (==) stay exact.

# --- Security-audited floors -------------------------------------------------
Django>=5.2.17
pillow>=12.3.0
djangorestframework>=3.17.2
aiohttp>=3.14.1
sqlparse>=0.6.0
requests>=2.32.4
protobuf>=5.29.6
h11>=0.16.0

# --- Security refresh (current secure versions) -------------------------------
# httpcore >=1.0.9 is required for the h11 floor to resolve (older httpcore
# caps h11<0.15, which carries the CVE the audit flags).
urllib3>=2.8.0
PyJWT>=2.14.0
idna>=3.20
python-dotenv>=1.2.3
httpcore>=1.0.9
# pypdf (rag: PDF text extraction). 6.12.1 is the first release clearing the
# 2026 advisories (PYSEC-2026-3004 fixed in 6.12.0; GHSA-wjqc-6w8f-h24c and
# PYSEC-2026-3025 fixed in 6.12.1) -- floor is the first OSV-clean version.
pypdf>=6.12.1

# --- Clean pins (unchanged) ---------------------------------------------------
# openai 2.54.0: floor set by livekit-plugins-openai 1.8.2 (openai[realtime]>=2.50);
# newest 2.x release so the agent-core chat surface stays on a single major line.
openai==2.54.0
numpy==2.1.2
gunicorn==23.0.0

# --- LiveKit family (1.x, modern agents API) ---------------------------------
# The voice worker uses the livekit-agents 1.x pattern (Agent + AgentSession +
# function_tool); 0.x is end-of-life for that surface. 1.8.x is the newest
# line whose plugins keep the audited pillow>=12.3.0 floor (their [images]
# extra no longer caps pillow) and support Python 3.10-3.14. The whole family
# moves together: agents 1.8.x requires livekit-api>=1.0 and
# livekit-protocol 1.1.x.
# livekit-agents 1.8.2 pins the rtc SDK exactly; keep the family in lockstep.
livekit==1.1.18
livekit-agents==1.8.2
livekit-api==1.2.1
livekit-plugins-openai==1.8.2
livekit-plugins-silero==1.8.2
livekit-protocol==1.1.27

# --- Other dependencies -------------------------------------------------------
aiodns==4.0.4
aiofiles==24.1.0
aiohappyeyeballs>=2.5.0
aiosignal>=1.4.0
annotated-types==0.7.0
anyio==4.6.2.post1
asgiref==3.8.1
async-timeout==4.0.3
attrs==24.2.0
# av floor is livekit-agents 1.8.x's requirement (>=14); the 13.x pin cannot stay.
av>=14.0.0
certifi>=2025.6.15
cffi==1.17.1
charset-normalizer==3.4.0
click==8.5.0
coloredlogs==15.0.1
distro==1.9.0
exceptiongroup==1.2.2
flatbuffers==24.3.25
frozenlist==1.5.0
httpx==0.27.2
humanfriendly==10.0
# jiter 0.10+ is required by openai 2.x.
jiter>=0.10.0
mpmath==1.3.0
multidict==6.1.0
onnxruntime==1.20.0
packaging==24.1
propcache==0.2.0
psutil>=7.0
pycares==5.0.1
pycparser==2.22
pydantic==2.9.2
pydantic_core==2.23.4
python-decouple==3.8
sniffio==1.3.1
sympy==1.13.3
tqdm==4.66.6
types-protobuf>=5.29.1
# typing_extensions 4.14+ is required by openai 2.x.
typing_extensions>=4.14
# watchfiles 1.0+ is required by livekit-agents 1.x.
watchfiles>=1.0
yarl==1.17.1

# --- Single-container deploy (Dockerfile) ------------------------------------
# Serves collected static files from inside the container (DEBUG=False safe).
whitenoise>=6.7.0
