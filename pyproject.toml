[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]

[tool.ruff.lint.per-file-ignores]
# Generated migrations routinely exceed the line length; they are not hand-edited.
"**/migrations/*" = ["E501"]
